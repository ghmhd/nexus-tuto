datasource db {
  provider = "sqlite"
  url = "file:./file.db"
}

generator prisma_client {
  provider= "prisma-client-js"
}

model Post {
  id Int @id @default(autoincrement())
  body String
  title String
  published Boolean
}
